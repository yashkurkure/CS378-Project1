name: cellular_automaton
description: A cellular automaton engine built from composable Dart mixins.
version: 1.0.0

environment:
  sdk: ^3.13.3

# Add regular dependencies here.
dependencies:

# Add dev dependencies here.
dev_dependencies:
